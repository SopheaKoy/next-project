all:
  children:
    servers:
      hosts:
        server:
          ansible_host: your_ip_server
          ansible_user: your_username
          ansible_ssh_private_key_file: your_ssh_key
  vars:
    env: local
